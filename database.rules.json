{
  "rules": {
    ".read": false,
    ".write": false,
    "leaderboard": {
      ".read": true
    },
    "users": {
      "$uid": {
        ".read": "!data.exists() || $uid === auth.uid",
        ".write": "!data.exists() || $uid === auth.uid"
      }
    }
  }
}
